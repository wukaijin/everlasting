# E2 frontend trace panel

## Goal

前端独立 trace 面板:useTraceStore(live+回看同构)+streamController 3 新 event case+<TracePanel>/<TurnTimeline>/<TurnCard>+AppShell drawer 挂载+渲染复用 parseAuditPayload/icon family+清理 UI。child-2,依赖 child-1 的 event+IPC+数据结构。

## Requirements

- TBD

## Acceptance Criteria

- [ ] TBD

## Notes

- Keep `prd.md` focused on requirements, constraints, and acceptance criteria.
- Lightweight tasks can remain PRD-only.
- For complex tasks, add `design.md` for technical design and `implement.md` for execution planning before `task.py start`.
