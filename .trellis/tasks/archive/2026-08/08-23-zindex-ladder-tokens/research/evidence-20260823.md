# 证据:08-23-zindex-ladder-tokens(2026-08-23)

## 迁移前(grep 实证)

72 处 z-index 声明、35 文件、21 个不同值(2~9999)。分带:
局部微层 2/5/10/20/50/60/70(10 处)、面板带 100(×4)、抽屉 105/110、
输入弹层 200(×3)、重覆盖面 999/1000(×3)、确认 1100(×2)/1200、
Modal 家族 2000/2001(×16)、portal 3000(×8,其中 6 处 !important)、
toast 5500、顶层 9999(×2:SessionList ctx 菜单 + AppShell 旧 toast)。

design-tokens.md "Modal Tokens" 表原写 backdrop 9998 / content 9999 /
toast 10000 ——与现实(2000/2001/5500)全面脱节,已修正。

## 落地

- style.css @theme 新增 13 档 `--z-*`(值=现网事实,零行为变更),
  注释写明"值即契约、新层级先查表、9999 是天花板别 +1"。
- 42 处跨组件站点扫编为 var(--z-*)(PRD 写 41 系漏数,实为 42),
  `!important` 原样保留;带断言脚本逐站校验旧值后替换。
- 局部微层 10 处留裸值:6 处补盖压契约注释,4 处原有(SubagentDrawer
  jump-latest、ChecklistCard 50、ChatPanel 60/70)。
- design-tokens.md:新增 "Z-Index Ladder" 节(13 档表 + 微层例外规则),
  Modal Tokens 表三行改为实际值并标注 stale 史。

## 验证(全绿)

- grep:组件里跨带裸值零残留。
- diff 数值保全自证:`git diff` 中 z-index 行删除/添加逐档等额
  (2000×8→overlay、2001×8→modal、3000!important×6、3000×2、200×3、
  100×4、9999×2、1100×2、其余 singles),无一错位。
- pnpm test 1175 全绿;pnpm build(vue-tsc+vite)零错。
- 运行时探针:开设置 modal,overlay computed z-index=2000、content=2001,
  证明 token 级联生效(probe-zindex.json / settings-modal.png)。

## 记录不处理(行为变更,另立任务)

- AppShell 旧 toast(projectsStore.toast,--z-top 9999)与 ToastProvider
  (--z-toast 5500)两套 toast 并存,合并需统一调用方。
- ChatPanel 浮动卡家族 50/60/70 是跨文件契约(ChecklistCard↔ChatPanel),
  靠注释维系;若将来家族扩员,考虑升格为 --z-chat-float-* 三档 token。
