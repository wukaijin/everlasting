{"file": "app/src/stores/streamController.ts", "reason": "核心修法位置：finalizeRequest（line 524-527）调 controller.evict(sessionId) + useChatStore().invalidateDiff(sessionId)。需读 handleToolCall/handleToolResult/handleChatEvent/evict/finalizeRequest 等函数确认调用顺序与边界条件；与现有 attachWorktree/detachWorktree/deleteWorktree 的 evict 模式保持一致"}
{"file": "app/src/stores/chat.ts", "reason": "新增 invalidateDiff(sessionId) action（diffCache.value.delete），从 streamController 的 finalizeRequest 调用。需读 fetchDiff/diffCache 当前实现确认 signature 一致；toPayloadContent（line 517-597）确认 assistant role 不发 toolResults 协议；send() 完整流程（line 599-673）确认 userMsg push + assistantMsg placeholder 行为"}
{"file": "app/src/stores/streamController.test.ts", "reason": "新增 vitest 锁住不变量：finalizeRequest 后 messagesBySession.has(sessionId) === false 且 loadedFromDb.has(sessionId) === false；finalizeRequest 后 chatStore.invalidateDiff 被调用（可 mock）。复用现有 rehydrateMessages 测试的 helper 风格"}
{"file": "docs/HACKING-llm.md", "reason": "追加'陷阱 4: in-memory placeholder 累积 vs DB 拆分 → 2013'章节，跟陷阱 1/2/3 措辞一致：现象 + 根因链 + 修法 + 验证。重点说明 c35c384 修的是 cancel 路径下 tool_result 缺失（陷阱 3），本任务是正常完成路径下 in-memory 形态不一致"}
{"file": ".trellis/spec/frontend/state-management.md", "reason": "追加 invariant：send 完成后 in-memory 必须 evict 跟 DB 同步。读现有 streamController ownership/lru/pinned section 了解风格（如果 spec 文件不存在则创建）"}
{"file": ".trellis/spec/backend/llm-contract.md", "reason": "Scenario 7 加一条 sub-section：'in-memory must mirror DB on send completion'。读现有 Scenario 7 风格（小节标题 + invariant + 9-test 表格）"}
{"file": "app/src-tauri/src/llm/types.rs", "reason": "参考 ContentBlock 序列化规则确认 wire 形态没问题（不在本任务改）。read_tool_result content 字段接受 JSON 字符串（envelope）以兼容 step 4 follow-up"}
{"file": ".trellis/tasks/archive/2026-06/06-08-06-08-step-4-followup-bugfix-2013-tool-use-orphan/prd.md", "reason": "参考上次的 PRD 风格（背景 + 根因 + 修法 + AC + Decision + Out of Scope）；确认上次的 c35c384 局限：cancel 路径 vs 本次正常完成路径"}
