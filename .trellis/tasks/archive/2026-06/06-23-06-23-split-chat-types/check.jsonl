{"file": ".trellis/spec/frontend/state-management.md", "reason": "Pinia store conventions — verify the split respects the documented facade pattern (chat.ts = UI-facing facade + streamController = single source of truth for streams). New chat.types.ts is a third file in the facade layer; check it does not create a circular import with streamController."}
{"file": ".trellis/spec/frontend/chat.md", "reason": "Verify the chat store API surface (the 14 exported types + MODE_CYCLE) remains unchanged — every consumer (MessageItem, ChatPanel, SubagentDrawer, etc.) must continue to import the same names from the same logical place."}
{"file": ".trellis/spec/guides/cross-layer-thinking-guide.md", "reason": "Cross-layer data flow guide — verify no layer boundary is broken when types move from chat.ts → chat.types.ts. streamController.ts imports types from chat; check no new import cycles."}