# AC2 视觉对照结论(2026-08-14,基线 215224 → 改后 230901)

基线:`research/vlm-report-20260814.md`;改后:`research/vlm-report-after.md`。
截图原件在 `out/ui-review/<ts>/`(gitignored)。

## 本轮覆盖类目的收敛判定

| AC2 类目 | 基线缺陷 | 改后 | 判定 |
|---|---|---|---|
| 长文密集感/行高(01) | "行间距明显偏紧…阅读压力大" | 该抱怨**消失**;新意见降级为装饰条权重、小标题层级等美学微调 | ✅ 收敛 |
| 微字号低对比(01) | "底部 LLM 1m 29.2s 极细灰字难识别" | **消失**(7 处 10px→11px 生效) | ✅ 收敛 |
| Tab 选中指示器(02) | "仅靠文字亮度区分,缺下划线"(基线即为误判,下划线已存在) | **消失** | ✅ 收敛 |
| "已加密保存"噪音(02) | "无意义占用空间" | **消失**(降级 tooltip) | ✅ 收敛 |
| 配对页:占位符区分/输入框风格/按钮态(07) | 三条全中 | 三条**全部消失**;剩余为功能性建议(自动分块/扫码,属 MVP out-of-scope) | ✅ 收敛 |
| 触控目标(06) | "目测 16-20px" | 仍报"视觉 24-28px" | ⚠️ **方法限制,非回归**:修复扩的是透明 hit area(视觉尺寸按设计不变),静态截图不可见;44px 已代码级核验(check 报告 AC1) |
| 顶栏收纳(06) | "桌面压缩版/图标堆叠" | 部分收敛(chips 已上移、群聊 chip 已隐藏),仍报"认知过载" | 🟡 部分收敛:进一步收纳(底部工具栏等)是结构性设计工作,超出本轮 CSS-only 范围 |
| 行高(06 移动端) | "行高过低" | 仍报 | ⚠️ 已知误判(research B1:正文 1.6,问题根源是节奏非行高,已修) |

## 桌面零回归(AC3)

01/02 改后报告的新意见(讨论总结装饰条过抢眼、URL 截断、分组标题层级等)均为**新增美学建议**,非样式回归;对照基线同类目无恶化。移动端专属改动全部隔离在 `@media (max-width: 767px)`(check 报告逐文件核验)。

## 方法论沉淀(供后续轮次)

1. VLM 静态截图评审**看不见**:透明 hit area、hover 态、动效、折叠分组内的状态(基线两条误判皆源于此)。触控/动效类验收必须代码级或交互级核验,VLM 报告只做参考。
2. VLM 对行高的抱怨在本项目语境下不可信(正文 1.6 仍连续两轮被判"过低"),其"密集感"反馈应转译为段落节奏问题排查。
3. 每轮评审的"剩余建议"(装饰条权重/URL 截断/分组标题层级/底部工具栏收纳)是下一轮 polish 的天然 backlog。
