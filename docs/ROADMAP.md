# ROADMAP — 技术路线图

> **维护承诺(2026-06-10 锁定)**:本文档是 living document,随功能完善 / 需求更改及时更新。**实施 / git log 是终极归档**(完整 commit 列表见 `git log --oneline -20`),本文档只列宏观计划。
>
> 文档职责:
> - ✅ 做什么 + 什么时候做(V2 4 档分类 + 已实施粗粒度归类)
> - ❌ **不**讲具体实现细节(实现走 [IMPLEMENTATION.md §1](./IMPLEMENTATION.md) / [ARCHITECTURE.md](./ARCHITECTURE.md))
> - ❌ **不**讲历史决策(决策走 [IMPLEMENTATION/decisions.md 决策日志](./IMPLEMENTATION/decisions.md))
>
> 需求见 [DESIGN.md](./DESIGN.md),架构见 [ARCHITECTURE.md](./ARCHITECTURE.md),技术选型见 [TECH.md](./TECH.md),实现讲解 + 决策日志见 [IMPLEMENTATION.md](./IMPLEMENTATION.md),候选功能见 [BACKLOG.md](./BACKLOG.md)。

---

## 1. 已实施(MVP 主体 + 路线图外完成)

> 粗粒度归类,不逐 commit 罗列。具体 commit 走 `git log --oneline -20`。

### 1.1 MVP 主体(原 7 步路线图)

<details>
<summary>8 项里程碑(全部 ✅)— 点击展开</summary>

| 阶段    | 里程碑 | 状态 |
|---------|--------|------|
| MVP     | 步骤 1:Tauri 2 + Vue 3 + Rust 骨架,LLM 流式对话 | ✅ |
| MVP     | 步骤 2:Tool Calling(`read_file` / `write_file` / `shell`)+ Agent Loop | ✅ |
| MVP     | 步骤 3a:SQLite 持久化 + Session 管理 | ✅ |
| MVP     | 步骤 3b-1:Project 数据模型 + 顶部 Tabs UI | ✅ |
| MVP     | 步骤 4:Git 集成(worktree + opt-in attach / detach / delete) | ✅ |
| MVP     | 步骤 5:WSL 体验(spike-001 验证) | ✅ |
| v1      | 步骤 6a:多 Provider(Anthropic / OpenAI,自研 Provider trait) | ✅ |
| 跨阶段  | 步骤 8:代码重构(5 PR — lib.rs / db.rs / 前端 sub-components / 文档 / STRUCTURE.md) | ✅ |

> 步骤 3b-2(完整三栏 UI + rig-core 迁移)已废弃,详见 [IMPLEMENTATION §4 决策日志 2026-06-09](./IMPLEMENTATION/decisions.md)。
</details>

### 1.2 路线图外完成

| 功能 | 日期 | 备注 |
|------|------|------|
| Anthropic extended thinking 块展示 + 持久化 | — | |
| spike-005 follow-up 7 PR | — | UI 紧凑 header / git_branch 显示 / 启动 batch backfill / pwd `~/` 简化 / write_file tracing / LLM cancel 机制 / markdown 渲染 |
| 字体栈调整(HarmonyOS Sans SC 子集打包) | — | |
| 6 UI/状态 bug 修复 | — | streamController 架构 + 顶栏窗口控制 + Markdown 表格 + Tauri 2 权限 + minimize icon + 顶栏 padding |
| 工具集扩展批次 | — | `edit_file` / `grep` / `glob` / `list_dir` + ReadGuard + Bash 落盘 + cat -n 行号 |
| provider catalog hot-reload + display_name optional + session model_id binding | 06-10 | |
| 体验优化批次 F1-F4 | 06-11 | per-project session 记忆 / 全程跟底滚动 / ConfirmDialog + 删除确认 / session 切换 loading + 双 IPC 修 + reloadAfterFinalize 抖动修 |
| **A4** Token 用量统计 | 06-10 | per-session 累积 + ChatInput hint 区展示 |
| **B5** Memory/指令文件系统 | 06-10/11 | 4 文件加载 + mtime fence 新鲜度校验(notify 已移除)+ `cache_control: ephemeral` 注入 + 前端 MemoryPreview UI + Settings Tab |
| **C1** 取消机制完整化 | 06-11 | tool 执行中途可取消 |
| **D1** session 重命名 + 8 色标记 | 06-11 | |
| **P0 工具打磨** | 06-12 | `read_file` offset/limit + `shell` timeout |
| **P1 web_fetch 工具** | 06-12 | 新增 8 号 tool:agent 自主抓取外部文档/API 参考/错误信息,SSRF 拦截 (RFC 1918/loopback/link-local/CGNAT/multicast/reserved + 169.254.169.254 短路),5 MiB body cap,30s timeout,htmd 0.5 转 markdown,attribution prefix (T1a prompt-injection 缓解)。PRD + 3 份 research 落 `.trellis/tasks/archive/2026-06/06-12-feat-tools-web-fetch-agent-api-p1/` |
| **C3** Context 压缩 + token 硬卡 | 06-12 | 5a 加载层 token 预算 + 超限降级(参见 [ARCHITECTURE §2.5.5](./ARCHITECTURE.md) + [ARCHITECTURE §2.2 ⑤](./ARCHITECTURE.md))。完整 PRD 走 `.trellis/tasks/archive/2026-06/06-12-c3-context-token/` |
| **A2 + B7** 权限系统 + 多模式(合并工作组) | 06-12/13 | ⑨ 关 5-tier 决策层(path-based,re-grill SOT)+ 3 档 Mode(`edit` / `plan` / `yolo`,`Background` enum 留位 UI 不暴露) + `match_kind` 3 种 wire 全连(`tool` / `prefix` / `path`)+ YoloConfirmModal + PermissionModal 路径范围行 + ⑯ 审计日志 17 类 AuditKind。`tools::web_fetch` 也接入 ⑨(Tier 4 走 `match_kind='tool'`) |
| **Mode 3 档化**(Q4 P2 后续) | 06-13 | `Mode::Chat → Edit` 改名 + `Mode::Review` 移除(行为跟 Plan 重复);v6 migration 启动时跑两次幂等 UPDATE;**breaking wire rename**,不保留 alias |
| **A7** RDP 双屏 position bug 修复 | 06-14 | 根因 = Wayland 禁止客户端 setPosition(WSLg/Weston 忽略,#14913 非可绕过),放弃手动铺满整屏,全平台改原生 `toggleMaximize()`;详见 [IMPLEMENTATION §4 2026-06-14](./IMPLEMENTATION/decisions.md) |
| **C4** 审计日志查询 UI | 06-14 | ⑩ `tool_executed` 落表(`record_tool_executed_audit`,payload `{tool_name, tool_input, duration_ms, exit_code}`)+ Tauri command `list_session_audit_events` + `useAuditStore` + `<AuditLogModal>`(reka-ui Dialog,绑当前 session,kind 下拉 + "仅 critical" 复选 + 计数 + 刷新 + 按 kind 分发渲染)。⑬ ⑮ 仍只 tracing(收益低)。完整 PRD 走 `.trellis/tasks/archive/2026-06/06-14-audit-log-query-ui/`,架构描述见 [ARCHITECTURE §2.5.8](./ARCHITECTURE.md#258-⑯-审计日志a2--b7-pr1--c4-pr1pr2-落地2026-06-1314已实施) |
| **RULE-E-006** worktree 路径对齐 Tauri `app_data_dir` | 06-15 | 删 `git::data_dir()` env-based 函数 + re-export + 模块 docstring,`AppState` 加 `app_data_dir: PathBuf` 字段(落在 data-plane group 内,保留 Grill decision #2 catalog-after-db 不变式),`attach_worktree` 从 state 取,worktree 与 SQLite DB 同根 `~/.local/share/dev.everlasting.app/`,`/tmp` fallback 消失。`cargo check` 0 warning,`cargo test --lib` 484/484 pass。完整 PRD 走 `.trellis/tasks/archive/2026-06/06-15-p1-worktree-data-dir-tauri/` |
| **B3** /command 命令面板 | 06-16/17 | 输入框行首 `/` 触发命令自动补全面板;内置(`/help` 列全部命令 / `/clear` 清空消息保留 session / `/new` 新建 session)+ 用户自定义(`.everlasting/commands/*.md` 手写 frontmatter parser 解析 `name`/`description`/可选 `argument-hint` + Markdown body 展开后作 user message 走 `send()`)。`<TriggerMenu>` 组件为 B2 @文件 / B4 skill 预置触发器骨架(共享 trigger char + 数据源注入)。`serde_yml`/`serde_yaml` 均废弃 → 通用 `ResourceLoader` 内置手写 parser(零依赖,字段简单时够用)。源优先级 builtin > project > user(project 覆盖 user 同名) |
| **B2** @文件补全 | 06-17 | 输入框 `@` 触发文件补全面板(fuzzysort 模糊匹配,复用 B3 `<TriggerMenu>` 第二 caller,与 `/command` palette 互斥)+ 后端 `files::walk_files`/`list_files`(gitignore + 默认排除 + 深度/数量上限)。CodeMirror 6 着色(@file `--color-tool-read` / /command `--color-accent`)。**后端 @token 注入文件内容**(对齐 CC/opencode/Aider/Cline,非路径提示):text 复用 `read_file` 截断(50KB head+tail + cat -n)注入,图片/PDF/Office/二进制**占位降级**(纯文本通道,multimodal 留 B1,文案引导 `pdftotext`/`pandoc`),无效路径(越界/不存在/不可读)保留原 token(email 不误伤)。二进制检测三层(NUL/非UTF-8/30% 控制字符)。6 家调研见 [docs/research/at-file-injection-coding-agents-survey.md](./research/at-file-injection-coding-agents-survey.md) |
| **D3** session 内消息编辑 / 重发 | 06-17 | 后端 `edit_user_message` 单事务(in-place 改写 + cascade 截断后续 + AuditKind)+ 前端 `MessageActionsMenu` + chat store `editMessage` + `MessageItem` edit mode + Resend 实质化(走 turn 边界 + agent loop 续编)+ `(edited)` 标签 + `AuditKind::ResendMessage` + RULE-A-010 spec 偏离声明(含 follow-up 点击无响应修复) |
| **B4** Skill 系统 | 06-18 | `use_skill` 虚拟 tool + 三层渐进披露(L0 清单独立 synthetic message 常驻 / L1 `tool_result` 回填正文 / L2 `read_file` 拉 reference),对齐 Claude Code `Skill` / Hermes `skill_view` 业界模式。加载层独立 `SkillCache`(复制 B3 `resource_loader` 模式,B3 零改动,唯一差异:skill 是 `<name>/SKILL.md` **目录**非单文件 → scan 走子目录)。frontmatter 最小集 name+description(复用 B3 手写 parser,`serde_yml` 已废弃)。修正 BACKLOG §2 两处过时(注入消息流非 system prompt)。MVP 纯 LLM 自动触发(无用户 `/skill`、无 `allowed-tools`)。调研见 [docs/research/skill-system-survey.md](./research/skill-system-survey.md),完整 PRD 走 `.trellis/tasks/archive/2026-06/06-18-skill-system/` |
| **B12** Checklist(agent 自跟踪进度清单) | 06-19 | TodoWrite 式 `update_checklist` tool(全量替换 + 三态 `pending`/`in_progress`/`done` + 至多一 in_progress coerce)+ loop-local Vec(per-request,handle 走 `ToolContext` 不改 `run_chat_loop` 14 参签名)+ 每轮 ephemeral 注入(**append** 到请求副本,不入持久化 messages,不破坏 memory cache 断点)+ 无新 DB 表(replay 从 DB history 还原,reload 按 `is_error` 过滤 cancel 合成 result)。前端 `<ChecklistCard>` ChatPanel 浮层(展开/最小化悬浮球 + 焦点动效)+ checklist store(客户端复刻 coerce)。先于 B6 subagent(注入机制小面 warm-up)。决策见 [IMPLEMENTATION §4 2026-06-18](./IMPLEMENTATION/decisions.md),术语见 [CONTEXT.md](./CONTEXT.md) |
| **L2** 单 turn 多 tool 并发执行(只读 batch) | 06-19 | `is_parallel_eligible` 纯谓词 + `FuturesUnordered`(每 task 内 check→execute→RULE-A-004 cancel 检查→audit→emit,`result_slots[i]` 按 tool_use 原始 index 回填 + `AtomicBool` 广播 cancelled)。并发集合 `{read_file,grep,glob,list_dir,use_skill}`(全静默 Allow,无 ask);排除 web_fetch(Tier4 默认 ask,Q2)+ 写类/shell/update_checklist。不变量:多 tool_result 单消息打包(parallel-tool-use 红线)+ RULE-A-004(cancelled 跳过 audit)+ execute_tool 签名未改 + 共享状态(`PermissionStore`/`SkillCache`/`ReadGuard` 均 Arc)并发安全。path-outside-root edge case 记 RULE-A-013(MVP 接受,follow-up 方案 a)。架构见 [ARCHITECTURE §2.5.9](./ARCHITECTURE.md#259-⑩-并行-tool-执行l2-mvp2026-06-19-落地已实施),调研见 [spikes](./spikes/2026-06-19-async-parallel-tool-research.md)。完整 PRD 走 `.trellis/tasks/archive/2026-06/06-19-l2-parallel-readonly-tool-batch/` |
| **L1** 后台 shell + 完成通知(L1a,不带 PTY) | 06-19 | `BackgroundShellRegistry` trait(Q1 决策 C,daemon 化换 impl 不动调用点)+ `InMemoryBackgroundShellRegistry` 进程内 impl(tokio 后台 task 拥有 Child,三触发 `select!`:`child.wait` / `kill_rx` / `sleep(max_runtime)`)。3 tool:`run_background_shell`/`shell_status`/`shell_kill`(Q2 Hermes terminal/process split);agent loop 每轮 `drain_notifications` + **APPEND** user message(Q3 opencode-pty 风格,APPEND 非 prepend 保 memory cache breakpoint,同 B12 checklist 不变量);通知仅 `exit_code`(Q4)LLM 主动 `shell_status` 拉;`max_runtime_ms` 默认 24h(Q6);session-scoped(Q7)+ `run_background_shell` Tier 4 Shell(同 shell,Plan 拦)/ `shell_status`·`shell_kill` Tier 5。复用 RULE-E-002 进程组 SIGKILL + RULE-E-001 safe_env(`apply_safe_env` 改 `pub(crate)`)。生命周期:`delete_session`→`kill_all_for_session`;`RunEvent::Exit`→`kill_all`。30KB spill + 1KB head/tail preview 复用 shell.rs。测试 651→680(+29)。Follow-up:`ShellEntry` 清理 sweeper([RULE-E-012](../.trellis/reviews/DEBT.md),P2)+ L1b 真 PTY(`portable-pty` + `pty_write` 交互式 dev server)+ L3 并行 subagent。完整 PRD 走 `.trellis/tasks/archive/2026-06/06-19-l1-shell-pty/`,spec 见 [tool-contract.md "Scenario: L1a Background Shell Tools"](../.trellis/spec/backend/tool-contract.md) |
| **B6** Subagent + SubagentDrawer redesign 收尾 | 06-20/21 | dispatch_subagent tool + worker agent(独立 context/token 预算)+ subagent_runs 持久化(RULE-A-014/015/016)+ transcript pairing + SubagentDrawer(B6 PR1-3 + FT-F-001~005)。**redesign 重构 PR1-6**(06-21):DB task/final_text 列 + RunAccumulator 删 chat_event 暴露 + MarkdownDetailModal/useTruncate + Drawer 视觉子组件 DrawerThinkingBlock/DrawerToolCallCard + 5 段分组折叠重写 + 边界态 cancelled/error/permission_ask。详见 [.trellis/spec/frontend/chat.md](../.trellis/spec/frontend/chat.md)。降级债(permission_ask interactive / cancelled turn N)见 [DEBT.md](../.trellis/reviews/DEBT.md) RULE-FrontSubagent-003/004 |
| **RULE-D-001** provider api_key 加密存储 | 06-24 | P1 安全债收口。AES-256-GCM + HKDF(machine-id) 派生 master key,AAD=provider id;新列 `api_key_enc` + `key_migrated_at` 哨兵 + 启动幂等迁移抹除旧明文。`ProviderRow.api_key` `#[serde(skip)]` 切断 IPC,`list_providers` 返 `hasKey`;前端编辑留空覆盖(None=保持/Some=覆盖)+ 加密徽标。否决 keyring(WSL 不可用)+ stronghold。威胁模型防 DB 文件泄露(无 machine-id 解不开)。完整 PRD 走 `.trellis/tasks/archive/2026-06/06-24-p1-api-key-encryption/`,决策见 [IMPLEMENTATION §4 2026-06-24](./IMPLEMENTATION/decisions.md) |
| **C2** 循环检测(agent loop ⑬ 关卡) | 06-24 | 架构预留、代码零实现的 ⑬ 关卡落地。**分级触发**取代原文单一 0.9 阈值(L1 精确签名硬触发 N=3 + L2 Jaccard 软提示 N=5/0.85),因单一阈值无法适配短/长 input。命中**软提示**:hint 注入 result message,LLM 下一轮看到,**不跳过执行、不终止 loop**(MAX_TURNS=200 兜底;**2026-08-19 起改软卡询问,见本表 softcap 行**,此为本行落地时的历史状态)。token 切分纯 Rust `split_whitespace`;edit_file 签名含 old_string 避免正当多块编辑误判。`agent/loop_detection.rs` + `chat_loop` 接入。完整 PRD 走 `.trellis/tasks/archive/2026-06/06-24-c2-loop-detection/`,决策见 [IMPLEMENTATION §4 2026-06-24](./IMPLEMENTATION/decisions.md) |
| **C2+** 循环检测主动干预 | 07-06 | C2 软提示不终止 loop 的死循环烧满 200 turn 敞口未堵。补中间主动层:per-`run_chat_loop`-local `loop_hit_count`,连续 3 轮 detect 命中 → 复用 `QuestionStore` + 前端 `<AskUserQuestionCard>` 询问"是否终止"(harness-driven,非 LLM tool 路径),三分支:终止 / 继续(count 清零 + 增强 hint)/ cancel。worker 触发直接 break 不弹 banner;新增 `AuditKind::LoopIntervention`(无 migration)。3 偏差(`run_id` 占位 / dispatch caller-append 不加 format 第 5 参 / worker 复用 `SubagentStatus::Incomplete` 不加变体)。完整 PRD 走 `.trellis/tasks/archive/2026-07/07-05-c2-loop-active-intervention/`,spec 见 [tool-contract "C2+ loop intervention"](../.trellis/spec/backend/tool-contract.md),决策见 [IMPLEMENTATION §4 2026-07-06](./IMPLEMENTATION/decisions.md) |
| **L3a** subagent 并发(只读 worker fan-out) | 06-24/25 | serial path 加 `DispatchBatch` 分类(Serial/OverLimit/Concurrent)+ 纯 dispatch 批(≥2)`FuturesUnordered` 并发(复用 L2 只读 batch 模板 `result_slots[i]`+`Arc<AtomicBool>`)+ 运行时 `force_readonly` 剥写(`READONLY_TOOL_ALLOWLIST` 只留 read/grep/glob/list_dir,只读保证第2层;第1层 SubagentDef allowlist,第3层 is_worker Deny 兜底)+ env `DELEGATION_MAX_CONCURRENT_CHILDREN` 默认3硬拒超限(对齐 Hermes)。**3 竞态点只读范围消解**(permission:ask is_worker 塌缩 Deny / token `col=COALESCE(col,0)+?` 原子增量 / cancel `child_token` fan-out)→ 零并发控制代码。`run_subagent` 加 `force_readonly` 参(serial 传false保护B6)。前端 store 按 runId 天然支持 N concurrent(PR2 实质满足)。spec 见 [tool-contract "Concurrent dispatch"](../.trellis/spec/backend/tool-contract.md) + [agent-loop "Concurrent readonly dispatch"](../.trellis/spec/backend/agent-loop-architecture.md)。**worker 联网(web_fetch)拆独立 task** `06-25-subagent-web-access`(L3a 验证发现 worker 三层不能联网)。完整 PRD 走 `.trellis/tasks/archive/2026-06/06-24-l3a-readonly-concurrent/`,决策见 [IMPLEMENTATION §4 2026-06-25](./IMPLEMENTATION/decisions.md) |
| **L3c** subagent 联网(worker web_fetch) | 06-25 | researcher `SubagentDef.tools` + 并发 worker `READONLY_TOOL_ALLOWLIST` 各加 `web_fetch`(第 1+2 层),第 3 层零改动。**基线验证推翻"is_worker Deny"前提**:worker ask 2026-06-22 已走 `WorkerAskBanner` round-trip(`ask_path` biased select),且 worker `PermissionContext.session_id`=父 session → `check_tool_grant` 已查父 session grant,故"父 session 授权过 web_fetch → worker 自动 Allow;无 grant → 弹 banner"天然工作。L3a 验证时 worker 报"无 web_fetch"纯是第 1+2 层剥掉工具。`READONLY_TOOL_ALLOWLIST` 加 web_fetch 不波及 L2(独立谓词 `is_parallel_eligible`)。并发 N worker 无 grant 时弹 N banner 接受现状(AllowAlways 不持久化,防跨权限边界),silent allow/持久化/配额作 follow-up。顺手修 LLM-facing `dispatch_subagent` description 过时"worker no UI"+ `tool-contract.md`/`dispatch.rs` 同款过时注释。864 测试全绿。完整 PRD 走 `.trellis/tasks/archive/2026-06/06-25-subagent-web-access/`,决策见 [IMPLEMENTATION §4 2026-06-25](./IMPLEMENTATION/decisions.md) |
| **L3d** subagent frontmatter loader(第三档收口) | 06-25/26 | 用户 `~/.config/everlasting/agents/*.md` + `<project>/.everlasting/agents/*.md` 定义 sub-agent;`SubagentCache` mtime fence 加载,project > user > builtin last-write-wins;`dispatch_subagent` 改每 turn `definition_with_cache` 动态拼 enum + source tag。**砍 PRD 的 `/reload-subagents` 命令**(mtime fence 自动 reload);防 worker 嵌套靠 `effective_is_worker` gate。**修订 PRD**:R1 user 路径 + R2 复用 Skill loader + R3 删"YAML fail-fast"伪命题。决策见 [IMPLEMENTATION §4 2026-06-26](./IMPLEMENTATION/decisions.md) |
| **L3b PR1** worker worktree 隔离核心(serial 路径) | 06-27 | `git::worktree::create_worker` / `destroy_worker` 变体(branch 前缀 `worker/<run_id>` + `git worktree lock` 跑期间 + self-heal 三态恢复);`SubagentDef.isolation` 字段 + `resolve_isolation` 双层合并;`run_chat_loop` 加 `worktree_override` / `app_data_dir` 参;`subagent_runs.worktree_path` 列 + `insert_run_with_id`;worker 完成 `diff_worker_worktree` 判 changes → 保留 branch + diff summary / 无则 destroy。spec 见 [tool-contract](../.trellis/spec/backend/tool-contract.md) + [worktree-contract](../.trellis/spec/backend/worktree-contract.md)。决策见 [IMPLEMENTATION §4 2026-06-27](./IMPLEMENTATION/decisions.md)。PR2-4 拆为 follow-up |
| **L3b PR2** concurrent dispatch 解锁 `force_readonly` → 各 worker worktree | 06-27 | `chat_loop.rs` concurrent 分支删 `force_readonly=true`(`run_subagent` 仍传 false);`force_readonly` 参保留 serial-only(L3a `single_dispatch_runs_serial_path_unchanged` 回归 + 未来「force read-only」opt-in feature 兼容);race-dissolution proof 重导(4 竞态点 — 新增 **worktree write race** = per-worker `worker/<run_id>` branch 消解;`permission:ask` 改 N banner 接受现状 + workaround pre-AllowAlways;token 用量 2026-06-26 reversal 不 fold 进父;cancel fan-out 不变);新增 3 个集成测试 + 1 个 L3a 测试改名(`l3a_concurrent_general_purpose_workers_complete_readonly` → `l3b_concurrent_general_purpose_workers_complete_shared`,加 `isolation: false` dispatch 入参复刻 L3a shared-cwd 行为);`make_harness_with_git_repo` 共享 helper(PR1 inline review 加,PR2 提升到 tests_common)。`run_chat_loop` 签名不变(concurrent 分支只调 `run_subagent` 的 `force_readonly=false`)。spec 改写:agent-loop "Concurrent readonly dispatch" → "Concurrent isolated dispatch" + race-dissolution 表重导 + tool-contract "Concurrent dispatch warning" L3b PR2 段取代 PR1 partial-mitigation 段。决策见 [IMPLEMENTATION §4 2026-06-27](./IMPLEMENTATION/decisions.md)。PR3-4 拆为 follow-up tasks(`merge_worker` + `discard_worker` tool + sweep / 前端 SubagentDrawer 合并/丢弃 UI) |
| **L3b PR3** `merge_worker` / `discard_worker` tool + sweep | 06-27 | 关闭 PR1 留下「有 changes worker branch 没人合并/丢弃」环:两个新 builtin tool(LLM-driven)+ 启动 sweep(过期 mtime 自动清理)+ Tauri command `merge_worker_run` / `discard_worker_run`。`merge_worker` libgit2 fast-forward / 3-way merge,冲突 → 返 conflict 文件列表;`discard_worker` 销毁 worker worktree + branch + clear 列;**MVP 不做幂等**。spec 增 `tool-contract` "merge_worker / discard_worker" Scenario + `worktree-contract` "Worker Worktree Sweep" Pattern。决策见 [IMPLEMENTATION §4 2026-06-27](./IMPLEMENTATION/decisions.md) |
| **L3b PR3+** permission/concurrency hardening | 06-28 | 审查 L3b 发现 PR3 三个 Blocker(均逐行验证):**B1** merge_worker/discard_worker 权限模型脱节(注释/spec 虚假声称 Tier4/Risk::High,实际 `ToolKind::Other`→Tier5 silent Allow + `Risk::Low` + Plan 模式可执行 merge 改写 parent 分支)→ 新增 `ToolKind::GitMutation` 变体(WebFetch 式:tool-level grant + ask;**不归 Shell** 避 `command` prefix-grant 空-token 串扰) + `risk_for_tool`→High + `filter_tools_for_mode` Plan 过滤;**B2** 并发 merge 无锁(`do_merge_blocking` 两 spawn_blocking 入口无互斥,libgit2 index 损坏风险)→ `merge_lock_for` per-`parent_session_id` `std::sync::Mutex`(`OnceLock<HashMap<String,Arc<Mutex>>>`)入口串行,discard 不加锁(不动 parent index);**B3** worker 越权(`STRUCTURALLY_DISABLED` 漏 merge/discard → worker 能 merge 兄弟 branch)→ 加两项。订正 merge_worker.rs/discard_worker.rs/tools/mod.rs 虚假注释 + tool-contract.md(权限段+并发段)。决策见 [IMPLEMENTATION §4 2026-06-28](./IMPLEMENTATION/decisions.md) |
| **L3b PR4** 前端 SubagentDrawer 合并/丢弃 UI | 06-27 | 闭合 L3b PR3 backend `merge_worker_run` / `discard_worker_run` IPC 在前端的可见/可控环。新增 `<WorkerBranchBadge>` (status + worktreePath 派生三态) + `<WorkerMergeControls>` (Merge/Discard 按钮 + ConfirmDialog 二次确认 + 冲突 inline 文件列表 + 0 i18n key 全中文);store 加 `mergeWorker` / `discardWorker` actions + per-run `mergeStateByRunId: reactive Map` spinner 隔离(多 drawer 互不阻塞);`parseConflictFiles` 正则提取 conflict 文件列表;`formatWorkerBranchLabel` util 把 `worker/<run_id>` → `Worker <8-char hash>`。**严格可见门**(不是单字段):`worktreePath != null && status === 'completed'` — cancelled/error/incomplete worker 不显按钮,即便 disk worktree_path 残留(L3b PR3 sweep 清后)用 worker exit-state 作权威信号。**Store cache 单源模式**:WorkerMergeControls 只接 `runId` prop,不接 `worktree-path`(`getRunCache` 是 SoT,`mergeWorker` 成功后 reactive `.set(runId, {...row, worktreePath: null})` → `v-if="visible"` 自动 false → 按钮消失,父无需 re-thread prop)。Icon.vue 加 `GitMerge`(lucide)。`vue-tsc --noEmit` 0 err + 597/597(初)→ 598/598(C5b 严格门 regression test 加后)vitest 全绿。spec 增:`frontend/chat.md` 加 "L3b PR4 SubagentDrawer merge/discard UI" 章节(新文件清单 + 严格门 + store 契约 + Conflict 跨层契约 + 单源模式决策) + `frontend/state-management.md` 加 "Per-run spinner isolation via reactive Map" Pattern + `backend/tool-contract.md` 加 "merge_worker conflict error string contract" 跨层契约(正则锁定)。决策见 [IMPLEMENTATION §4 2026-06-27](./IMPLEMENTATION/decisions.md) |
| **V2 2期** agent 自主记忆系统(`autonomous-memory` epic, spike-007) | 06-29 | 把 `memory/` 从"开发者手写指令文件加载"升级为 **agent 自主产生 + 跨 session 召回的经验库**。5 child(P1 存储/P2 读写闭环/P3 工具执行前召回/P4 事件驱动自动写入/P5 质量层)。两层召回(per-turn FTS5 + 工具前 trigger_key 精确匹配)+ 状态机 candidate→active→verified(靠 hit_count+存续)+ verified 软拦截重判(P5,动 loop)+ 异步卫生 job(char-trigram dedup/age-out)。spec 见 [backend/memory.md](../.trellis/spec/backend/memory.md) Scenario 2,决策见 [IMPLEMENTATION §4 2026-06-29](./IMPLEMENTATION/decisions.md) |
| **B9** 生成式 UI(部分落地:selector/diff/code_block) | 07-02 | parent `07-02-b9-generative-ui` + 3 child(A use_ui 基础设施 / B code_block / C diff)。D1-D6 决策(use_ui 单 tool non-blocking;selector 复用 ask_user_question;button/diff应用/开关推后期)。后端 `tools/use_ui.rs`(non-blocking execute,Tier 5 silent Allow 同 remember)+ 前端 `<UiCard>` + component registry(`type`→组件,MessageItem dispatch `tool_name===use_ui`)。code_block=hljs(`lib/common`)+marked-highlight(顺带 markdown 代码块高亮)+复制;diff=复用 DiffView 只读(parsePatch 拆多文件→FileDiff[])+复制;selector=复用 ask_user_question(零代码)。**推后期**:独立 button+action 白名单(D3,安全关键)/diff 应用(D4)/session 开关(D5)/自由式 UI。spec 见 [tool-contract use_ui](../.trellis/spec/backend/tool-contract.md) + [frontend/chat UiCard](../.trellis/spec/frontend/chat.md),完整 PRD 走 `.trellis/tasks/archive/2026-07/07-02-b9-generative-ui/`,决策见 [IMPLEMENTATION §4 2026-07-02](./IMPLEMENTATION/decisions.md) |
| **B9+** 生成式 UI 收尾(D3 通用 button + D4 diff 应用) | 07-13 | B9(07-02)只读落地的收口:**D3 通用 button primitive**(`use_ui` schema 加 `button` type + `KNOWN_BUTTON_ACTIONS` 白名单 + 前端 `<ButtonPrimitive>` 3 action 分发)+ **D4 diff 应用闭环**(新 IPC `apply_ui_diff` — user 触发非 LLM tool,plan 模式天然可用;`assert_within_root` 校验 + 手写 unified diff parser/applier 零依赖 + 逐 FilePatch fail-fast + `AuditKind::UiDiffApplied`)。**三角色分离**:LLM 仅提议 / 用户点击 = 授权 / 后端 IPC 走 boundary+audit。spec 见 [tool-contract use_ui + apply_ui_diff](../.trellis/spec/backend/tool-contract.md) + [frontend/chat UiCard](../.trellis/spec/frontend/chat.md),决策见 [IMPLEMENTATION §4 2026-07-13](./IMPLEMENTATION/decisions.md) |
| **B6+ A** subagent 多模型 — frontmatter `model:` 声明 | 07-03 | `SubagentDef` 加 `model: Option<String>`;loader frontmatter `model:` 从 Q4 warn+discard 翻转为 store;`run_subagent` 加 catalog 参数 + 纯函数 `resolve_worker_provider`(catalog 命中换 provider + `get_model` 取 context_window/display;miss/None `warn!` 降级 parent);catalog 经 `app_subagent_catalog(&app_handle)` 从 `AppState` 取(**`run_chat_loop` 23 参签名不动**,3 调用点 body 内现取);`format_dispatch_result_with_model` 加 `[model: <name>]` 行(3 参 wrapper 保留为 `#[cfg(test)]` 兼容)。动机:跨模型对抗性 review(抗同模型自确认偏差)。AC6(thinking 跨模型)由 provider caps 自动跟随 + 显式测试留 follow-up。B(动态选模型)/C(UI 配置 + id↔name 友好解析)留本档 `B6+`。完整 PRD 走 `.trellis/tasks/archive/2026-07/07-03-subagent-frontmatter-model/` |
| **B6+ C** subagent 多模型 — Settings UI + builtin DB override + 写回 frontmatter | 07-03 | 补 A 任务两块缺口:① builtin agent 无 frontmatter 文件可改 → `subagent_model_overrides` 表(global `agent_name → model_id`,优先级 `DB > frontmatter > parent`);② frontmatter 写 `models.id` 不友好 → Settings UI `<SubagentsTab>`(per-row model 下拉 + 失效标红 + builtin 行 "(DB override)" + `set_subagent_model` IPC 行级写回)。`model_display` 列加到 `subagent_runs`(card + drawer 显示 chip)。spec 增 `subagent-runs-schema` "B6+ C additions" 段。决策见 [IMPLEMENTATION §4 2026-07-03](./IMPLEMENTATION/decisions.md) |
| **B6+ B** subagent 多模型 — dispatch 动态选模型 | 07-06 | 补 A(声明性 frontmatter)/C(声明性 DB+UI)之外的**临时性**单次 dispatch 覆盖,优先级链收口 **dispatch > DB > frontmatter > parent**。两入口:① LLM path — `dispatch_subagent` tool schema 加 `model` 可选参(动态 enum 值=display_name,`definition_with_cache` turn-0 快照 `list_models` 构建 `ModelOption{id,display_name}`;会话内 CRUD 滞后可接受 + 失效兜底);② user path — `@@<agent> --model=<id\|display_name> <task>` 前缀(`chat.ts` 正则,flag 紧跟 agent 名、task 之前;前端 `useModelsStore` 反查 display_name→id,多同名取首 + `console.warn`,经 `ForcedDispatch.model_id` 透传)。后端 `resolve_model_by_name_or_id`(id passthrough → display_name reverse lookup)+ `dispatch_model.or(resolve_final_model)` 单点接入(`resolve_final_model`/`resolve_worker_provider` 签名零改动,A/C 零回归)。失效兜底(model 被删/拼错/反查无果)→ 不失败,`warn!` + 降级 `resolve_final_model`。临时 override 不持久化(不写 DB/frontmatter,永久改走 Settings UI/C);`subagent_runs.model_display` + tool_result `[model:]` 行与 A/C 一致(零特例)。完整 PRD 走 `.trellis/tasks/archive/2026-07/07-06-b6plus-b-dispatch-model-arg/` |
| **A2+** shell 命令只读/副作用精细判定(P1+P2 同 PR) | 07-04 | 收 A2+B7 三档分类两缺口 + 一个独立漏判:① **R1 复合命令 grant 绕过** — `ls; rm x` 首 token=ls 命中 Tier 4 prefix-grant 短路直 Allow 跳过 classify;② **R2 只读管道误伤** — `git diff \| head` 一刀切降级 Ask;③ **R3 `>` 写重定向静默放行** — 结构检测不查 `>`,`git diff > patch.txt` 在 Plan 模式静默写文件。改造:`has_structural_metachar`(v1 不引号感知,false-positive 安全)gate check.rs 两处 grant 短路合一((a) prefix-grant + worker run-grant);`classify_prefix` 入口重写为 命令替换(`$()`/反引号一律 Ask)→ 自研 4 态拆分器(顶层 `;`/`&&`/`\|\|`/`\|`,引号/转义感知)→ 每段 `classify_single`(复用现有 first-token + 三张表 + git 子命令)+ `detect_write_redirect`(`>`/`>>`/`&>` 升 SideEffect;`2>&1`/`>&N` fd 复制 / `<` 输入不升)→ 取 max(`ShellTrust::severity` + `max_of`,不 derive Ord)。单 commit(代码耦合 + design §6 回滚单元=整个 PR)。七不变量全保持。完整 PRD 走 `.trellis/tasks/archive/2026-07/07-04-a2-shell-p1p2-classify/`,方案见 [docs/A2-SHELL-CLASSIFICATION.md](./A2-SHELL-CLASSIFICATION.md),决策见 [IMPLEMENTATION §4 2026-07-04](./IMPLEMENTATION/decisions.md) |
| **A5+** LLM 网络健壮性(重试 / Full Jitter / SSE 首字节边界) | 07-05 | provider 层补 `retry_open` wrapper(`llm/retry.rs`)堵 5xx/429/网络断连整轮失败。① **Full Jitter** 退避(AWS 共识 `uniform(0, min(cap, base·2ⁿ))`,纯指数会让并发客户端聚集)+ retry-after advisory 优先(5 格式:retry-after-ms / retry-after 秒·HTTP-date / OpenAI x-ratelimit-reset Go duration,60s 二次封顶)+ 双向独立熔断(max_retries × budget);② **首字节前重试边界**(对齐 Claude Code "before visible output"):`OpenOutcome::Stream` 一旦返回永不重试,后续 stream Err 走 chat_loop had_error(tool 在 stream 后执行,首字节前重发零副作用,不需幂等 key);③ `LlmError` `RateLimit`/`Server` 加 `headers: HeaderMap` 字段(5 类名称/分类不变,非破坏,不入 DB);④ 前端 `ChatEvent::Retrying` chip(↩ 重试中 N/M,Ts 后重发…),瞬态不入 messages/rehydrate;⑤ R9 token 不重复(retry 首字节前失败不 emit Done → `update_last_turn_usage` 只记成功 turn 一次,直查 SQL 验)。调研 `docs/research/llm-network-resilience-survey.md`(§5.4 证实 SSE 无 resumption,"message ID 续传"退路不可行 → 整请求重发安全边界)。spec 见 [llm-contract A5+](../.trellis/spec/backend/llm-contract.md),完整 PRD 走 `.trellis/tasks/archive/2026-07/07-04-a5plus-llm-network-resilience/`,决策见 [IMPLEMENTATION §4 2026-07-05](./IMPLEMENTATION/decisions.md) |
| **E1** CI 测试自动化管线(GitHub Actions) | 07-05 | V2 第三档 E1 落地,首次上 CI。双 job 并行(rust: apt 装 webkit2gtk-4.1-dev 等 + dtolnay/rust-toolchain stable + Swatinem/rust-cache + `cargo fmt --check` + `cargo test --lib` / frontend: pnpm 10 + node 22 + cache + `pnpm install --frozen-lockfile` + `pnpm test` + `pnpm build` 含 vue-tsc)。触发 push:[main] + PR + workflow_dispatch;paths-ignore 忽略 `**/*.md`/`docs/**`/`.trellis/**`;concurrency cancel in-flight 省 GH 分钟。reqwest 用 rustls 省 libssl-dev;vendored-libgit2 靠 rust-cache 兜(~60s/次)。CI 首跑暴露 2 个预存 flaky(项目此前无 CI,本地 N 次侥幸过):① **background shell drain race**(生产代码修复 `in_memory.rs::drain_notifications`)— destructive pop 与 shell 完成 push 竞速,echo fork+exec+exit+push(几 ms)可能晚于 turn 切换(μs)→ drain pop 空队列,notification 延迟或丢失(**真实生产 race**:快 shell + loop 早结束 → notification 永不 drain,LLM 不知道 shell 完成);修复 = 队列空 + 近期(<200ms)running shell 时 yield+poll(5ms,cap 100ms),dev server(>200ms)不受影响,队列非空/无 running 立即返回(原行为);② **loader mtime fence 精度**(测试侧修复)— 两次连续 fs::write 间隔过短 FS mtime 精度不足区分(ext4 ns 但 overlay/tmpfs + 并行负载弱化 delta),原 sleep 15ms 在写 v2 之后对 v2 自身 mtime 无效,改 spin until mtime 真推进(cap 2s,确定性)。本地 3x 全量 1274/1274 + 718 vitest + vue-tsc 0 err + fmt 干净。clippy 留 follow-up(先本地清 warning 再加 gate)。完整 PRD 走 `.trellis/tasks/archive/2026-07/07-05-ci-test-automation/`,决策见 [IMPLEMENTATION §4 2026-07-05](./IMPLEMENTATION/decisions.md) |
| **V2-2+** 自主记忆可观测性 + 管理面板 | 07-06 | V2 2 期自主记忆(06-29)全是 agent 自主闭环(写 / 召回 / 反思 / 提升 / 卫生);本档补**人**的入口 — 看召回命中、手动转状态、人工编辑、删过时记忆。**Phase A 后端**(14 文件):migration 加 `edited_by_user` 列(default 0,agent 写;1=人工编辑 provenance);提取 `validate_memory_text` 安全网 helper(insert + update 共用,堵人工绕过);新 `update_memory` + 2 IPC;`ChatEvent::Recall`(只读 / 非持久化,同 `Retrying`)+ 3 emit 点(FTS turn start / pitfall ×2);sibling `build_recall_text_with_rows` / `recall_pitfall_with_hits`(原 fn 退化为 thin wrapper,P3/P4/P5 闭环零回归);worker sink **结构隔离**(AC7:`SubagentBufferSink` 无 chat-event emit 路径,recall 永不冒泡到主 chat)。**Phase B 前端**(10 文件):`recallHitsBySession` reactive Map 落 `useMemoryStore`(不归 streamController,spec 硬规则)+ `handleChatEvent` 路由 + `startRequest` 清空(per-turn);`RuntimeMemoryModal.vue`(reka-ui Dialog,状态机 Select 驱动 `LEGAL_STATUS_TRANSITIONS` 前端只读副本 + backend 矩阵硬墙 + 转降级弹 reason + 编辑 + 删除);ChatPanel recall chip(语义/陷阱分组);MemoryPreview 行加 hitCount/lastUsedAt chip + 「人工编辑」徽标 + 行点击 emit manage。1300 后端 + 757 前端全绿,vue-tsc 0 err,fmt 干净。spec 见 [backend/memory.md §V2-2+](../.trellis/spec/backend/memory.md),完整 PRD 走 `.trellis/tasks/archive/2026-07/07-06-am-observability-panel/`,决策见 [IMPLEMENTATION §4 2026-07-06](./IMPLEMENTATION/decisions.md) |
| `request_mode_change` tool — LLM 申请切 mode(用户 inline card 授权) | 07-07 | LLM 在主 loop 可申请 `target_mode ∈ {edit, plan, yolo}`(+可选 reason),user 经 inline card allow/deny;允许路径复用 `set_session_mode` 副作用链(DB + 审计 + Yolo 二次 modal),拒绝路径回灌 `{"cancelled_by_user": true}`。`PendingInteraction` tagged enum(`Question | ModeChange`)单 pending gate 互斥;3 类新 audit(`mode_change_requested/allowed/denied`),AuditKind 17→20;worker 禁用。关键落地决策:tool 内部不直接落库,走"前端触发 IPC → `resolve_mode_change` → `set_session_mode`"避免双路径漂移。详见 [决策日志 2026-07-07](./IMPLEMENTATION/decisions.md),完整 PRD 走 `.trellis/tasks/archive/2026-07/07-07-07-07-request-mode-change-tool/` |
| **B8** 可编排(DAG workflow) 编排层 | 07-08~10 | workflow.json 外置(`.everlasting/workflow.json` + `load_workflow` + `validate` + `fallback`)+ builtin dev workflow plugin(`resources/builtin-workflow/dev/workflow.json` 开箱即用,新项目零配置)+ Step 0.1~3.3 完整 9 阶段管线(`workflow_enabled` 列 / 顶栏 toggle / `WorkflowDef` struct / `task.json` 读写 / `create_task` IPC / per-turn breadcrumb 注入 / plugin skill loader 4 步 / delegation 模板 / role-gate / `set_task_state` + `archive_task` IPC + `TaskStatus::Completed`)+ plugin agents/ 落点(`SubagentSource::Plugin`)+ task 状态机(Planning → Implement → Check → Done 四态单向)+ `B12 Checklist → task.json.items` 同步 + 07-09 chip merge(`<WorkflowChip>` 合并 WorkflowToggle + PluginSelect)+ 07-09 transition-card(前端 `<TransitionCard>`)+ 07-10 task.json hardening(R1-R5:read_task lenient + `create_task` LLM tool + 即时 resolve + 软推荐 hint)。详见 [IMPLEMENTATION §4 2026-07-08 workflow 系统总览](./IMPLEMENTATION/decisions.md),主任务 `.trellis/tasks/archive/2026-07/07-08-workflow-integration/`(9 阶段 25 commit),延伸 tasks `07-09-workflow-builtin-plugin/` / `07-09-workflow-chip-merge/` / `07-09-workflow-transition-card/` / `07-10-workflow-task-json-hardening/` |
| **pending-indicator** 跨 session 待处理交互 UI 三档提醒(角标 + 徽章 + toast) | 07-08 | `ask_user_question` / `request_task_state_transition` 跨 session 消息携带 pending 标记时,前端三档提醒:① **角标**(Sidebar session 项 红点,持久);② **徽章**(顶栏 workflow chip 旁 小数字,即时全局计数);③ **toast**(切到其他 session 时,唤回「session X 有 N 个未答问题」)。pending 标记随消息持久化(经 store 层,不入 ChatEvent),session 维度去重(N 个消息算 1 次,避免计数爆炸)。与 chip-merge / transition-card 同期落地。详见 [IMPLEMENTATION §4 2026-07-08 pending-indicator](./IMPLEMENTATION/decisions.md),commit `b95e8c5 feat(pending-indicator): 跨 session 待处理交互 UI 提醒(角标+徽章+toast 三档)` |
| **E2** turn-level harness trace viewer | 07-14 | parent `07-14-e2-harness-trace-viewer` + 2 child(后端管道 / 前端面板)。补 ROADMAP 4 维 trace 缺口(C3 压缩 / per-turn token / C2 soft hint / workflow breadcrumb)+ 审计 turn 对齐。**后端**:v7 migration(`turn_trace` 表 UNIQUE(session_id,seq) + `session_audit_events.turn_seq` 列)+ 3 新 ChatEvent(`ContextCompacted`/`LoopHint`/`WorkflowBreadcrumb`)+ `agent/trace.rs` 双写 helper + `db/trace.rs` CRUD + `record_audit_event` 扩 turn_seq + `list_turn_traces`/`clear_session_trace` IPC。**前端**:`<TracePanel>` AppShell drawer(live+回看同构)+ `useTraceStore` + `<TurnTimeline>`/`<TurnCard>` + streamController 3 case。3 决策:scope 完整版 / 独立面板 live+回看 / always-on 落盘+清理。不动 agent 决策逻辑(trace 旁路观测)。spec 见 [backend database-guidelines turn_trace](../.trellis/spec/backend/database-guidelines.md) + [frontend state-management traceStore](../.trellis/spec/frontend/state-management.md),决策见 [IMPLEMENTATION §4 2026-07-14](./IMPLEMENTATION/decisions.md) |
| **daemon 化**(remote-access epic,5 Phase) | 07-20~23 | agent core 从 Tauri GUI 进程拆出为独立 `everlasting-daemon` 进程(axum HTTP server),GUI 作为瘦客户端 spawn daemon 为 sidecar,前端默认 `httpTransport`(同源 HTTP/SSE)通信;daemon 用 ServeDir 同源服务 SPA,支持纯浏览器访问(浏览器模式)。`?transport=tauri` + Full 模式是 daemon 故障逃生舱(回退 legacy in-process Tauri IPC)。**5 Phase 落地**:① transport 抽象(07-20,Tauri IPC 与载体解耦);② axum daemon P2.1+P2.2(76→79 REST 路由镜像 #[tauri::command]);③ SSE event stream P2.3(`HttpSseSink` + `/api/v1/stream`);④ sidecar spawn + httpTransport 默认 + ServeDir + thin client P2.4(`sidecar.rs` GuiMode{Thin,Full} + `RunEvent::Exit` 回收);⑤ E2E harness + transport parity 测试 P2.5。后续修 `resolve_data_dir` 对齐 Tauri identifier + `scripts/daemon.sh` 浏览器模式管理。架构见 [ARCHITECTURE §1/§4](./ARCHITECTURE.md),结构见 [STRUCTURE.md](../STRUCTURE.md),编排放 [REMOTE-ACCESS-ROADMAP.md](./REMOTE-ACCESS-ROADMAP.md),决策见 [IMPLEMENTATION §4 2026-07-20](./IMPLEMENTATION/decisions.md)。任务 `07-20-remote-access-daemon-split/` + `07-20-remote-access-transport-abstraction/` |
| **B11 远程遥控通道**(remote-control epic) | 08-11~13 | **S1~S6b 全落地**(08-13 合入 main):`crates/everlasting-remote` 云服务端(axum 0.7 ws:auth shared_secret/device_token、配对码 60s 一次性 + per-IP 限速、WSS 隧道、反向代理、SSE 桥、DB nodes/devices/pairing_codes)+ `crates/everlasting-remote-protocol`;PC daemon tunnel client(`daemon/tunnel/`:WSS 长连接 + loopback 转发,**agent core 零改动**;取消只停转发 sse_bridge `select!`);前端 vue-router + PWA 壳 + 配对/节点视图 + `transport/auth.ts` pwa-remote 模式 + RemoteTab;移动端适配(S5 抽屉导航/S6a 主聊天视图/S6b 设置面板 + 真机迭代);**Cargo workspace 翻转**(根 `Cargo.toml`,default-members 只含 remote 两 crate,`Cargo.lock` 移根)。部署见 [REMOTE-DEPLOY.md](./REMOTE-DEPLOY.md),E2E 验收见 [REMOTE-ACCESS-E2E.md](./REMOTE-ACCESS-E2E.md),PRD 走 `.trellis/tasks/08-11-remote-control-epic/`,决策见 [IMPLEMENTATION §4 2026-08-11~13](./IMPLEMENTATION/decisions-2026-08.md) |
| daemon graceful shutdown 加固 | 07-25 | 闭合硬终止缺口:① 主动 cancel + drain agent loop(SIGTERM 不再卡住等 SIGKILL);② 主动关 SSE 长连接(`HttpSseSink` graceful);③ sidecar 孤儿进程清理(`RunEvent::Exit` 钩子兜底)。修复 `serve_daemon` 3s 自杀回归。决策见 [IMPLEMENTATION §4 2026-07-25](./IMPLEMENTATION/decisions.md) |
| **交错思考渲染**(interleaved thinking) | 07-23/24 | 后端落库保留**真实流序**(thinking/text/tool_use 按 BlockState 到达序,而非 Anthropic 的 "text 先于 tool_use" 分组)+ 前端 run 分组 + contentBlocks 时间轴渲染。修复 Anthropic thinking 块中途消失 + 实现真·工具穿插(工具在思考之间执行)。设计见 [docs/INTERLEAVED-THINKING-DESIGN.md](./INTERLEAVED-THINKING-DESIGN.md) |
| **C2** review-state 矩阵视图(review-viz) | 07-26 | review plugin 产出的 `review-state.json` 三态载荷(维度×发现矩阵)前端可视化:`<ReviewMatrix>` + `<ReviewMatrixGrid>`(维度×发现网格)+ `<ReviewFindingDetail>` + `<ReviewDimensionCompare>`;`reviewState.ts` store + `streamController` 的 `tool:call` 路由刷新;后端 `commands/review.rs`(get_review_state + get_current_task_slug)。决策见 [IMPLEMENTATION §4 2026-07-26](./IMPLEMENTATION/decisions.md) |
| lefthook pre-commit(cargo fmt / lockfile 同步拦截) | 07-26 | 引入 lefthook(`lefthook.yml`)在 pre-commit 阶段拦截 `cargo fmt --check` + pnpm-lock.yaml 同步,堵 "忘记 fmt / lockfile 漂移" 进仓。spec 沉淀为强制约定 |
| ask_user_question 自由输入(allow_custom + skip-semantics) | 07-26 | `ask_user_question` 加 `allow_custom` 选项(用户除预设 options 外可自由输入)+ skip-semantics(自由输入回灌 `is_error: false`,与 deny 的 `is_error: true` 区分)。任务 `07-29-ask-user-question-custom-input/`(archive) |
| subagent resume mechanism(C1)+ TaskStatus 自定义 plugin state(C0) | 07-28 | review epic 前置基建:① **C0** `TaskStatus` accommodate custom plugin state(支持非 builtin workflow plugin 的自定义状态);② **C1** resume mechanism for worker runs(中断的 subagent run 可续跑)。Merge feat/subagent-resume-c1 |
| **群聊 group chat**(turn-taking 编排引擎) | 07-29~08-07 | `session_type=group_chat` 时走 `agent/group_chat_loop.rs`(编排)+ `group_chat_prompts.rs`(prompt/role_history)多参与者 turn-taking 编排:moderator 主持 + 参与者轮流发言,`nominate_speaker`/`end_discussion` 工具控制轮次,每条 message 带 `speaker` 列,前端按 speaker 渲染独立气泡 + 实时发言人 chip。**4 Phase**:① 数据层 + wire 层 speaker 维度;② turn-taking 编排引擎;③ speaker 落库/读取;④ 创建群聊 session + 参与者配置 UI + 逐轮流式。08-04 编排重写(入口持久化去重 + 参与者身份护栏 + 终止/发言人事件);08-06 废弃 round-robin fallback(moderator 未提名则重试 turn,不机械派人)+ wire 层孤儿自愈;08-07 工具白名单收敛(仲裁工具 moderator-only,参与者/主持人均只留调研类 `read_file/grep/glob/list_dir/web_fetch`)+ per-role history 隔离(`role_history`,治同模型串台)+ 参与者 max_turns 1→20 + `end_discussion` 总结卡片 + identity_contract。任务走 `.trellis/tasks/archive/2026-07/07-29-group-chat/` + `archive/2026-08/08-0{4,6,7}-group-chat-*`,决策见 [IMPLEMENTATION §4 决策日志](./IMPLEMENTATION/decisions.md) |
| **前端 vendor 分包 manualChunks** | 08-11 | vite `build.rollupOptions.output.manualChunks` 按运行时角色拆 vendor:主 app chunk 1.08 MB → 344 kB(消除 >800 kB chunk 警告),vendor 按 vue/reka/editor(markdown/编辑器/图标/misc)分组独立缓存。顺带消除 vite `dynamic import will not move module into another chunk` 噪音警告(questionCards↔chat 初始化期解环是有意的) |
| **C7** tools[] 上下文 token 治理 | 08-14 | 把 `tools[]` 数组当作与 messages 并列的**上下文治理对象**(省窗口预算,provider 无关;cache 断点只省钱不省窗口)。**R1 度量**:`turn_tool_defs` freeze 后对序列化 post-filter ToolDef JSON 跑 cl100k(`memory/tokens.rs` 的 `count_tokens`),落 `turn_trace.tools_token` 新列(幂等 migration helper `add_turn_trace_column_if_missing` + `upsert_turn_trace_token` 扩参)+ `<TracePanel>` TurnCard tools cell(占比 = tools_token / context_input,**不 double-count** —— context_input 已含 tools)。**R3 静态裁剪**:`filter_tools_for_session_type` 经典聊天砍群聊专属 `nominate_speaker`/`end_discussion`(~465 tok/轮,落实旧注释 "Phase 4 may filter"),drive.rs 过滤链第三环 mode→workflow→session_type;群聊 no-op(`group_chat_tool_defs` 白名单优先)。**R2(Anthropic cache 断点)+ D(Stub 注册)降 Phase 2**:relay 实测 `cache_creation=0` 零收益(等原生 Claude provider);D 触发线 = tools 占窗口 >15%。**live 烟测(08-14,重编 release + 重启 daemon 实跑单轮)**:tools_token=6773 / context_input=17602 = **38.5%,D 触发线已过**;同轮 memory 指令块估算 ~7-8k(≈42%,CLAUDE.md 27.8KB + AGENTS.md 3.2KB 实测),见 [BACKLOG §3.1 待办](./BACKLOG.md)。完整 PRD 走 `.trellis/tasks/archive/2026-08/08-14-c7-tools-token-governance/`,调研见 [tool-context-progressive-disclosure](./research/tool-context-progressive-disclosure.md),spec 见 [token-usage-tracking §C7](../.trellis/spec/backend/token-usage-tracking.md) |
| **C7D** tools Stub 注册(C7 Phase 2 之 D,渐进式披露方向②) | 08-14 | C7 触发线(38.5% > 15%)已过后落地:`tools/stub.rs` 把 10 个大 schema 候选工具(`STUB_CANDIDATES`,use_ui/ask_user_question 等)首轮原地替换为 stub(真名 + 一句话摘要 + 宽松外壳 `{"type":"object"}`),另注册常驻 `load_tool_schemas` 元工具按需取回完整契约再真实调用;session 粘性 `StubRegistry`(loaded-set,`delete_session` 清空)+ 开关 `tools_stub_enabled`;drive.rs 过滤链第 4 环,gate = 开关 && 非 worker && 非群聊(群聊白名单语义 / worker 自主可靠性豁免)。live 两轮复验:首轮 tools_token 6773→**3677**(-45.7%,占首轮 context 38.5%→26%);AC1 阈值 3000→3700 校准(dispatch def 984 tok 不可省)。PRD 走 `.trellis/tasks/archive/2026-08/08-14-c7d-tools-stub-registration/`。后续:memory 指令块(~7-8k ≈ 42%)反超为最大头,治理任务 `.trellis/tasks/08-15-memory-block-governance/` |
| **memory-gov** memory 指令块窗口治理(WP1 度量 + WP2 分级注入 digest) | 08-15 | 把 memory 指令块(CLAUDE.md/AGENTS.md 4 层)纳入与 messages/tools 并列的窗口治理。**WP1 度量**:`turn_trace.memory_token` 列(幂等 backfill,与 tools_token 同 Done 写点/同 upsert 契约)+ init.rs 对实际注入 blocks(banner+wrappers+bodies)cl100k 估算经 `LoopInit`→`drive_turn` 落库 + TurnCard `mem` cell(占比 = memory_token/context_input,不 double-count)+ `turn-smoke.sh` 报告列与 `--turns N`(AC4 双轮 cache 对比入口)。**WP2 分级注入**(同构 C7D 渐进披露):`memory/digest.rs` fence-aware 切节 + 目录(标题+首句,纯机械);tier = AGENTS.md(primary)永不 digest / CLAUDE.md(reference)且 tokens>600 才 digest;`load_memory_sections` 元工具(banner label 命名空间寻址,精确→唯一前缀/子串;drive 侧挂 append,tools.rs serial 顶部拦截,独立于 stub gate);粘性 `MemoryDigestRegistry`(OnceLock 单例,已加载节追加在目录后保前缀;`delete_session_inner` 清理);gate `memory_digest_enabled`(缺省 on,fail-open)&& !worker && !群聊(worker 路径 subagent/prompt.rs 一行不动;digest_off 与 legacy 逐字节一致,单测锁)。**live 实测(08-15)**:memory 10124→**2080**(-79.5%,占首轮 72%→28%),首轮 context 14079→7421(**-47%**);双轮 cache 率 99.8% vs off 99.7% 不劣化;定向探针确认模型按目录主动拉节(Tech Stack)并遵循。完整 PRD/design/implement 走 `.trellis/tasks/08-15-memory-block-governance/`,spec 沉淀 [memory/decisions](../.trellis/spec/backend/memory/decisions.md) + [token-usage-tracking](../.trellis/spec/backend/token-usage-tracking.md) |

| **B1** 图片支持(multimodal) | 08-16/17 | 输入层升级为图片通道:**粘贴暂存列表 + @文件真注入**两路入口(交互:粘贴不即发,缩略图暂存随消息发出,允许纯图发送,内存态切走即清);`models.supports_images` 配置项(对齐 supports_thinking 模式,Settings 可编辑 + picker vision tag);存储走文件系统 `attachments/<session_id>/` + `messages.metadata` 引用 + daemon **首个二进制 GET 路由**(浏览器/PWA/Tauri 同源直载,pwa-remote 走 proxy+query token);wire 层 `ContentBlock` 双形态(ImageRef 引用全管线轻量 / Image serde 即 Anthropic 原生,OpenAI image_url data URL)+ caps=false **占位降级**(模型知道有图未发,防幻觉——live 实证);`turn_trace.images_token` 第三治理切片(口径=请求内全部图含历史重建,评审 P0-1 修正);两级张数闸(新图/轮≤10 + 请求总量≤20);@图副本 + 魔数校验 + imagesize 头探测 (w×h)/750;群聊参与者全可见(reload_messages 从 metadata 重建);**顺手闭合既有安全缺口**——DOMPurify 原配置放行外链 `<img>`,现仅自家 attachments 前缀三态放行,外链图降级链接(BACKLOG §3.3 不变量首次真正成立)。worker/read_file 读图豁免(follow-up:拖拽/自动压缩/工具读图)。live:turn-smoke img 列 + 800×600 图 640 tok 精确落值 + 降级路径模型正确拒答;正向视觉路径待真实 vision 模型(catalog 现无)。5 PR:2b647c0/c7ebf62/9403de0/71e1d5d/3a2abc3。PRD/design/review 走 `.trellis/tasks/08-16-b1-image-multimodal/`,spec 沉淀 [llm-contract Image Blocks](../.trellis/spec/backend/llm-contract.md) + [token-usage-tracking images_token](../.trellis/spec/backend/token-usage-tracking.md) |
| **D2①** 跨 session 全文搜索(用户驱动 MVP) | 08-17 | 全局 Cmd/Ctrl+K SearchModal(AppShell 层;移动端 AppHeader 搜索按钮 + 全屏降级):跨全部 project 检索 `messages.text`(thinking 天然排除)。**后端**:`messages_fts` FTS5(external-content + trigram;update trigger 限定 `AFTER UPDATE OF text` 防高频写放大 + `%_docsize` 影子表作陈旧探针)+ `db/search.rs` 共享查询层(≥3 字符 FTS/bm25 + <3 字符 LIKE 兜底 → 2 字中文可搜)+ `search_messages` 6 处注册链(POST)。**前端**:单 Modal 两态(结果列表 ↔ 只读预览,复用 `MessageItem` 新增 `readonly` prop)+ `buildRunGroups` 纯函数 + `openSessionInProject` 组合 action(先 `switchProject` → `loadSessions` → `switchSession`)。live 实测:2 字中文跨 3 project 命中 / 存量回填 1192=1192 精确同步。② Agent 驱动 `search_history` tool 同日落地(见下一行)。spec 沉淀 [database-guidelines messages_fts](../.trellis/spec/backend/database-guidelines.md) |
| **D2②** Agent 驱动 `search_history` tool | 08-17 | `tools/search_history.rs` 薄封装复用 `db::search::search_messages` SQL 层(零 SQL/IPC/前端改动);`{query, scope: all\|current_project, limit≤50}` → 紧凑一行一 hit 文本。权限链零改动(`ToolKind::Other` Tier 5 silent Allow);`READONLY_TOOL_ALLOWLIST` 第 6 员;非 C7D stub 候选但注册 +178 tok → C7D AC1 预算线二次校准 3700→3900(「新工具先评估扩 STUB_CANDIDATES」守则)。spec 沉淀 [tool-contract 15-search-history](../.trellis/spec/backend/tool-contract/15-search-history.md)。**+前端专属卡片**(`08-17-search-history-card`):SearchHistoryCard 替换渲染 + useSearchModal prefill 扩参 |
| **C3+** 摘要式上下文压缩(LLM 摘要取代机械丢组) | 08-18 | C3(06-12)机械丢组的语义升级,去硬卡/手动 /compact/handoff 的前置管道。**两层结构 + 兜底链**:超 0.85×window(turn 边界)→ LLM 结构化摘要(9 段模板 + handoff 前缀 + `prior-summary` 增量合并 "conversation wins")+ 保留区逐字存活(`clamp(15k, 10%窗, 25k)`,组边界对齐,typed-user 护栏);失败 → 机械丢组原样降级 → StillOver fail-fast(RULE-A-002 不变);连续 3 次失败熔断(进程级 registry,同 digest 先例)。**持久化与水位(本任务核心难点)**:摘要行落 `messages` 表(metadata kind=compaction_summary,content/text **两列同值**纯摘要,前缀话术不落库);**水位按 `cutoff_seq` 精确折叠**(非摘要行位置 —— check P1 发现按位置折叠会丢保留区与本请求提问);查 DB 不信 wire(wire 无 metadata);seq 吃 loop 游标(独立 MAX+1 会撞主键);D3 cascade 自愈;FTS 自动索引(D2 可搜)。scope gate 同 digest(`llm_compaction_enabled` 缺省 on && !worker && !群聊);前端摘要行最低渲染(低调系统行 + 展开)+ TracePanel method 徽标。1813 后端(1 预存 flake)+ 1099 前端 tests 绿,clippy/fmt/vue-tsc 干净。PRD/design/review/研究走 `.trellis/tasks/08-18-llm-context-compaction/`(7 家工具调研见 research/02-04),spec 沉淀 [agent-loop pattern-llm-compaction](../.trellis/spec/backend/agent-loop-architecture/pattern-llm-compaction.md) + [database-guidelines compaction_summary](../.trellis/spec/backend/database-guidelines.md) + [token-usage-tracking 摘要旁路 usage](../.trellis/spec/backend/token-usage-tracking.md)。live 烟测待真机(重编 daemon 后构造超线长 session,AC1/AC2 live 半边) |
| **unified-context-budget** 统一 token 预算(WP1 度量 + WP2 关卡⑤硬卡) | 08-19 | C7/memory-gov/B1 切片齐备后的预算收口。**WP1 度量**:`turn_trace` 三新列 — `at_files_token`(@文件注入体 cl100k 估算,span 寻址单一定义 `apply_span_placeholder` 委托 `span_text` 校验)/ `system_token`(system prompt 体 + skill-listing 合成消息)/ `context_window`(请求时模型窗口快照,前端预算行分母);**压缩口径统一切换** — 触发线 / 摘要 postcheck / 机械 compact 三处全改用"发送部件加法"(`count_tokens(system) + count_tokens(tools_json) + estimate_messages_tokens(messages)`,`agent/budget.rs::estimate_request_tokens`),修复旧口径只数 messages 漏计 tools/system 的洞;**核心不变量**归因切片(tools/memory/@files/images/system)与总量永不互相加计。**WP2 硬卡引擎**:`BUDGET_LINE_RATIO=0.95`×window 触发静默裁剪,裁尽仍超才 fail-fast;触发落 `AuditKind::ContextBudgetTrim`;trace 实发口径对齐。**前端**:TurnCard 预算构成条(per-model 窗口)+ BudgetTrim 瞬时 chip + 审计条目。3 PR(ca675a1 WP1 / 325ad19 WP2 / 415c2fa 前端)+ span 寻址重构(76d5eb7)+ 质检修正(6bfcf86)。spec 沉淀 [pattern-budget-gate](../.trellis/spec/backend/agent-loop-architecture/pattern-budget-gate.md) + [token-usage-tracking 新 Scenario](../.trellis/spec/backend/token-usage-tracking.md)。完整 PRD 走 `.trellis/tasks/archive/2026-08/08-19-unified-context-budget/` |
| **MAX_TURNS 软卡** 撞线询问替代硬终断 | 08-19 | 单聊主 loop 撞线(缺省 `MAX_TURNS=200`)不再无条件 `stop_reason="max_turns"` 硬停,改 QuestionStore 软卡询问:继续(+`TURN_LIMIT_GRANT`=200,400/600… 每次撞线再问)/ 压缩后续跑(置 `force_compaction=true` 绕过 C3 token 触发线,`trigger_label="softcap"` 观测区分)/ 停止;10 分钟超时兜底。**break 门 = `effective_is_worker \|\| group_chat_state.is_some()`**:worker(有 C1 resume)与群聊 speaker 段保持硬卡直接 break(漏群聊门会让 tool_use 结尾的 speaker 轮挂满超时)。新增 `AuditKind::TurnLimitSoftcap`(action `asked/continued/compacted_continued/stopped/timeout_stopped/cancelled`,worker 与群聊不落表);`PendingInteraction::TurnLimitSoftcap` 前端复用 `AskUserQuestionCard` 浮动卡(`tool_use_id` 前缀 `turn_limit_softcap_{turn}`,绝不能 tag 成 `Question`)。实现 `chat_loop.rs::ask_turn_limit_softcap` + `emit_max_turns_terminal`(worker break 与软卡停止共用)。PRD/design/research 走 `.trellis/tasks/archive/2026-08/08-18-max-turns-softcap/`,spec 沉淀 [pattern-turn-limit-softcap](../.trellis/spec/backend/agent-loop-architecture/pattern-turn-limit-softcap.md) |
| **手动 /compact 命令** 空闲期摘要压缩入口 | 08-19 | C3+ 的主动触发补充:空闲期(loop 非活跃)调 LLM 摘要压缩,替代"只能等超 0.85 触发线自动压"。通用内置命令直输分发(`/compact`),`CompactResult.method`(Summary/Mechanical/None)观测 + `summary_usage` 不混入 `update_last_turn_usage`(只进 compaction_json)。与软卡「压缩后续跑」force 路径互斥:软卡走 drive_turn 按值穿参(seq=MAX+1 契约要求空闲期,loop 活跃调用会与 persist 撞号)。PRD 走 `.trellis/tasks/archive/2026-08/08-18-manual-compact-command/` |
| **handoff 跨 session 接力** | 08-19 | C3+ 的「handoff 前缀」落地为产品功能:接力摘要带进下一 session + **修复 HUD 按 session 隔离**(接力状态不再串 session)。PRD 走 `.trellis/tasks/archive/2026-08/08-18-handoff-mechanism/` |
| **worker per-turn 度量** turn_trace 并入 run 维度 | 08-20 | 把 E2 turn-level trace 从"只记主 loop"扩展到 worker per-run:worker 每轮 token 落 `turn_trace` run 维度行。**表重建迁移** `UNIQUE(session_id, seq)` → `UNIQUE(session_id, run_id, seq)`(`''` 哨兵 = 主 loop 行,worker 行 = `subagent_runs.id`;**不用 NULL** 因 SQLite UNIQUE 视 NULL 互异,会插出第二行破坏 upsert)+ partial index `idx_turn_trace_run`(`WHERE run_id != ''`);老库走 `schema_helpers::rebuild_turn_trace_with_run_id`(表约束加宽迁移守则,见 spec)。**写点归位**:Done 臂 run 行落值 + compaction / loop_hint 旁路写点带 run_id;db 四 upsert 扩参与双路读。**IPC 全链**:`list_worker_turn_traces`(按 run_id 点查)+ 集成回归锁。**前端**:SubagentDrawer「Token 明细」per-run worker 轮 token 折叠区 + `runTracesByRunId` 粘性缓存。spec 沉淀 worker per-turn 行语义 + subagent_runs×turn_trace 关联。PRD 走 `.trellis/tasks/archive/2026-08/08-20-worker-turn-trace-persist/` |
| **B1 收尾** 图片自动压缩 + 拖拽 + read_file 工具读图 | 08-21 | 粘贴/拖拽入口 canvas 压缩(长边>1568 降采样;无透明且>1MB 重编码 JPEG q0.85;压后判 5MB;fail-open)+ 聊天区拖图入暂存(非图片 toast 引导 @)+ `read_file` 读图(白名单+魔数+5MiB 闸 → attachments 副本 + `ToolResult.images` 引用)。**ToolResultData 双形态 serde**(DB=refs / wire=Anthropic tool_result content block array;无图路径逐字节不变 fixture 锁);OpenAI-compat 与 caps=false 占位降级;`estimate_images_token` 内联 tokens_est 精确计费 + budget 臂 2 覆盖;前端 tool result 卡缩略图(主消息流 + SubagentDrawer,attachmentUrl 路由)。live:MiniMax-M3 read_file UI 截图准确描述内容(**正向视觉路径首次 live 实证**),images_token=1728 精确入账。任务 `.trellis/tasks/08-21-b1-image-followups/`,spec 见 llm-contract §Tool-Result Image Blocks + tool-contract/16 + token-usage-tracking 工具图计费 |

---

## 2. V2 路线图分类(2026-06-10 重排)

> 2026-06-13 收尾更新。

### 🟢 第一档 — ✅ 已全部完成(2026-06-10/11,本档收口)

> A4 / B5 / C1 / D1 四项均已落地，详见 §1.2 已实施列表。

### 🟡 第二档 — ✅ 已全部完成(2026-06-12/13/14/17,6 项进 §1)

| 编号 | 功能 | 备注 |
|------|------|------|
| ~~A2 + B7~~ | ~~权限系统 + 多模式(合并工作组)~~ | ✅ 06-12/13 落地,见 §1.2 |
| ~~C3~~ | ~~Context 压缩 + token 硬卡~~ | ✅ 06-12 落地,见 §1.2 |
| ~~B3~~ | ~/command 命令面板~ | ✅ 06-16/17 落地,见 §1.2 |
| ~~C4~~ | ~~审计日志~~ | ✅ 06-13/14 落地,见 §1.2(⑨ ⑩ 写入 + 查询 UI)|
| ~~B2~~ | ~@文件补全~ | ✅ 06-17 落地(PR1+PR1.5+PR2),见 §1.2 |
| ~~D3~~ | ~session 内消息编辑 / 重发~ | ✅ 06-17 落地(PR1+PR2+PR3+follow-up),见 §1.2 |

### 🟠 第三档 — 缓做(active 项)

| 编号 | 功能 | 备注 |
|------|------|------|
| ~~B9~~ | ~~生成式 UI~~ | ✅ 07-02 部分落地(selector/diff/code_block,button 推后期),见 §1.2 |
| ~~B9+~~ | ~~生成式 UI 收尾(button + action / diff 应用)~~ | ✅ 07-13 落地(D3 通用 button + D4 diff 应用 + UiDiffApplied 审计),见 §1.2 |
| C6   | 大输出截断统一 | ⑩ ⑫ 边界处统一处理 |
| ~~B1~~ | ~~图片支持(multimodal)~~ | ✅ 08-16/17 落地(粘贴+@双入口/wire 双形态/占位降级/images_token 切片/DOMPurify 收紧),见 §1.2 |
| ~~D2~~ | ~~跨 session 全文搜索(双驱动)~~ | ✅ 双驱动 08-17 全部落地:① 用户驱动(全局 SearchModal + messages_fts + 只读预览定位)+ ② Agent 驱动 `search_history` tool(复用 `db::search` 查询层,不走 IPC),见 §1.2 |
| A5/A6 | 错误处理完善 + README + demo | 打磨 |
| ~~E1~~ | ~~CI 测试自动化管线(GitHub Actions)~~ | ✅ 07-05 落地(双 job 并行 + CI 首跑暴露的 drain race / mtime fence 2 个 flaky 修复),见 §1.2 |
| ~~A5+~~ | ~~LLM 网络健壮性(重试 / 指数退避 / SSE 断点续传)~~ | ✅ 07-05 落地(retry_open + Full Jitter + 首字节前重试;SSE 无 resumption 故不走 message ID 续传),见 §1.2 |
| **A4+** | 成本聚合视图(token → $) | A4 per-session token 累计已有;补跨 session / provider / day 汇总换算,纯前端聚合查询不动 agent core |
| ~~V2-2+~~ | ~~自主记忆可观测性 + 管理面板~~ | ✅ 07-06 落地(`update_memory` + `validate_memory_text` helper 提取 + `ChatEvent::Recall` 只读 event + worker sink 隔离 AC7 + `RuntimeMemoryModal` 状态机/编辑/删除 + ChatPanel 实时召回 chip;2 commits Phase A 后端 / Phase B 前端),见 §1.2 |
| ~~E2~~ | ~~turn-level harness trace viewer~~ | ✅ 07-14 落地(后端 trace 管道 + 前端独立面板 live+回看,4 维缺口全补),见 §1.2 |
| ~~C2+~~ | ~~循环检测升级为主动干预~~ | ✅ 07-06 落地(per-run-local `loop_hit_count` N=3 + QuestionStore 复用 + 三分支 + worker `effective_is_worker` 直接 break + `AuditKind::LoopIntervention` 无 migration),见 §1.2 |
| ~~A2+~~ | ~~shell 命令只读/副作用精细判定~~ | ✅ 07-04 落地(P1+P2 同 PR),见 §1.2 |
| ~~B6+~~ | ~~subagent 多模型支持（A frontmatter / B 动态选模型 / C UI+DB override）~~ | ✅ A 07-03 / C 07-03 / **B 07-06 全落地**，优先级链 `dispatch > DB > frontmatter > parent`，见 §1.2 |
| ~~L3b PR1~~ | ~~worker worktree 隔离核心(PR1 落地,见 §1.2)~~ | 06-27 PR1 已落地,见 §1.2;PR2-4 拆为 follow-up tasks |
| ~~C7~~ | ~~工具上下文渐进式披露(tools[] token 治理)~~ | ✅ 08-14 落地(R1 度量 + R3 静态裁剪;live 实测 tools 占首轮 context 38.5% → D(Stub)Phase 2 触发线 >15% 已过),见 §1.2 |
| **F1** | 消息队列(输入排队 / 优先级 / 批量注入) | 当前 turn 串行,占用时新输入只能阻塞/丢弃;补显式**输入侧**队列(排队 + 优先级 + 批量注入),统一外部入口(用户连发 / 群聊 / F2 定时任务共用),复用群聊 turn-taking 编排基建。区别于 daemon SSE **输出侧** backpressure(已落地,见 [REMOTE-ACCESS-ROADMAP §P2.3](./REMOTE-ACCESS-ROADMAP.md)) |
| **F3** | 资源治理(系统级限损框架) | context/token 治理已落地(unified-context-budget / C7 / memory-gov / B1,见 §1.2);扩展为统一资源治理:并发(worker / shell 上限)、进程 / 内存、磁盘(worktree / attachments / 日志),与 F1 反压联动。**边界**:不含 Provider API 限流(C5 已移除,见 §3) |
| **F4** | Web 搜索工具 | 现有 `web_fetch` 需已知 URL,查最新文档 / API 变更 / 报错解法无入口;补"搜索 → 取前 N 条结果正文"组合,复用 web_fetch 安全模型(SSRF 拦截 / 5 MiB cap / htmd 转 markdown / attribution),主流 coding agent 标配 |
| **F5** | PDF/Office 文档阅读 | B2 @文件对 PDF/Office 仍占位降级(`agent/at_file.rs` 仅提示 shell 跑 pdftotext / pandoc);补文本型 PDF 原生文本注入 + 扫描件走 B1 图片通道。B2 输入层遗留缺口,与 B1(图片)同属多模态家族 |

> **已实施的 22 项**(B6 / B6+ / B8 / B12 / B4 / C2 / C2+ / A7 / L2 / L1 / L3a / L3b PR1 / L3b PR2 / L3b PR3 / L3c / L3d / A2+ / A5+ / E1 / V2-2+ / E2 / C7)已从第三档或第四档移到 §1.2 已实施列表。

### 🔴 第四档 — 最远远期(app 主体完善之后)(4 项,B8 已于 2026-07-10、B11 已于 2026-08-13 迁至 §1.2)

| 编号 | 功能 | 备注 |
|------|------|------|
| B10  | 飞书 IM | daemon 化已于 2026-07 作为独立基础设施落地(见 §1.2 "daemon 化" epic);B10 现可基于既有 daemon + transport 抽象推进,不再是"重大架构变更"阻塞。本档只评估飞书 channel 接入 |
| ~~B11~~ | ~~远程遥控通道(原"云端同步 Cloudflare Workers + D1")~~ | ✅ **08-11~13 已实施**(remote-control epic S1~S6b,08-13 合入 main),见 §1.2。中继方案:国内 2C2G 服务器 + 自研 Rust remote daemon;不做主动推送、不做多用户、不做跨节点同步 |
| A2+ P3 | shell 执行期沙盒兜底(bubblewrap/overlayfs/firejail) | A2+ P1+P2 **判定层** 07-04 落地(见 §1.2);P3 是判定层之下的独立**限损层** — 判定错了也限损(盲区 `VAR=val cmd` / `$var` 展开 / 拆分器引号极端误判靠它兜底)。前置 WSL userns spike。拆自 parent `07-04-a2-shell-classification`(已 archive,P1+P2 收口)。源方案 [docs/A2-SHELL-CLASSIFICATION.md](./A2-SHELL-CLASSIFICATION.md) §4 远期候选 |
| F2  | 定时任务(本地 cron 式) | 本地定时唤醒 agent 跑任务(定时拉取 / 定时汇报);触发源守 [DESIGN 硬约束](./DESIGN.md#32-明确不做硬约束)(系统时间 / fs 事件 / 本地 webhook,**不接云端触发器**)。注入点走 F1 消息队列,空闲调度依赖 F3 并发上限 |
| **F6** | 异步 agent 任务(detach 后台跑) | agent 级异步执行:大任务(重构模块 / 跑完测试汇报)后台跑、期间用户干别的、完成通知;daemon 化后技术上可行,缺任务状态管理 UX。与 F1 队列 / F2 定时 / F3 并发同属异步执行家族,依赖 F1/F3,建议统一设计 |

---

## 3. 移除项 / 已废弃(V2 重排,2026-06-10 决定)

> **不再做**的项目归这里,避免认知噪音。决策日志已覆盖"为什么不做"。

### 3.1 移除(明确不做)

| 编号 | 项目 | 一句话原因 |
|------|------|------------|
| A1   | xterm.js 嵌入式终端 | v1 `shell` tool + 30K 落盘已覆盖"看 agent 在跑啥"的需求 |
| A3   | MCP 暴露 | 个人工具,工具集对外开放是 Claude Code 生态已经解决的问题,本项目杠杆不足 |
| C5   | Provider 限流(令牌桶) | 个人使用场景未撞到限流;v1 之后看实际用量再评估 |

### 3.2 已废弃(历史决策,保留归档)

- **3b-2 完整三栏 UI + rig-core 迁移** — rig-core 0.38.1 弃用(2026-06-09 决策,自研 `Provider` trait 已完整支持多 Provider),3b-2 同步废弃
- 决策依据见 [IMPLEMENTATION §4 决策日志](./IMPLEMENTATION/decisions.md)对应日期条目

---

## 4. 关键理解纠正(必须留笔,2026-06-10)

### 4.1 B6 = Subagent(**不是**用户切角色)

- **正确语义**:main agent 在 ⑥ LLM 决策后,派出一个 **worker agent** 跑独立 context(独立 messages / 独立 token 预算),完成后由 worker 把 **summary** 回填给 main agent
- **类比**:Claude Code 的 Task tool / OpenHands 的 subagent
- **harness engineering 学习价值高**:消息流隔离、context 预算管理、summary 注入位置,都是 harness 设计的核心命题
- **依赖**:B5 Memory 落地后(worker 需要 user/project memory 上下文)再做,效果最佳

### 4.2 B7 = Mode 是 A2 权限系统的 UX 层

- **正确语义**:B7(mode = `edit` / `plan` / `yolo`)**不是**独立功能,是 A2 权限系统的**前端 UX 层**;`Background` enum 留位但 UI 不暴露
- **历史演进**:2026-06-12 落地 4 档(`Chat` / `Plan` / `Review` / `Yolo`),2026-06-13 grill-with-docs session 3 档化(`Chat → Edit` 改名 + `Review` 移除,行为跟 `Plan` 重复);详见 [IMPLEMENTATION §4 决策日志 2026-06-13 "Mode 3 档化"](./IMPLEMENTATION.md)
- **联动链**:前端 mode 切换 → 后端 ARCHITECTURE §2.2 **⑧a Mode 检查**(plan 模式拒 tool_use / yolo 跳过 ⑨ Tier 4 弹窗但 Tier 2 硬墙仍生效) + ⑨ 权限检查 联动
- **工作组划分**:A2 + B7 合并做(基础设施 + UX 一组),已进 §1.2 已实施

### 4.3 A2 + B7 合并工作组(2026-06-12/13 完成,已进 §1.2)

- A2(后端 ⑨ 权限基础架构) + B7(前端 mode 切换 UI)是一组工作,不能拆
- 实施顺序:先 A2 后 B7(B7 依赖 A2 暴露的 mode 配置),3 档化(Q4 P2 后续)单列 ADR

---

## 5. 后续维护承诺

- **本文件改动时机**:
  - 完成 V2 任何一档任何一项 → 移到 §1 已实施(列"做了什么 + 时间",细节走链接,不写 commit hash)
  - 重新审视 V2 档位(升档 / 降档 / 移除) → 直接编辑 §2 / §3 + 在 [IMPLEMENTATION §4 决策日志](./IMPLEMENTATION/decisions.md) 追加 ADR 条目
  - V2 → V3 重排 → 整体替换本文件或归档到 `docs/_archive/`
- **不做的边界(新增 / 编辑本文件时的自查点,与头部维护承诺一致)**:
  - ❌ 不列具体 commit / PR 编号(具体 commit 走 `git log`,日期即索引)
  - ❌ 不写测试数 / token 数字 / 实现机制等技术细节(具体设计走 BACKLOG.md / 各 spec 文件 / 对应 PRD 归档)
  - ❌ 不做决策追溯(走 IMPLEMENTATION §4 决策日志)
  - ✅ 新行 = 一句"做了什么 + 时间 + 链接(到 spec / PRD / 决策日志)"
- **其他文件引用本文件的统一形式**:`[docs/ROADMAP.md §X](./ROADMAP.md#X)`,不复制路线图内容到其他文件
