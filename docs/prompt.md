## bug
1. 前端顶栏双击空白处可以最大化和回归之前的尺寸，但是右侧的按钮点击无反应
2. 2880*1920的屏幕下，最大化窗口只能是`1920*1080`，贴着屏幕左上角。手动拖动窗口边缘，可以超过这个尺寸。
3. 顶栏最小化和最大化的按钮（关闭窗口按钮左边两个）的图标不对
4. 顶栏logo 没有右边距
5. message text markdown 渲染的 table 边框 cell border 很淡，看不太清
6. llm 流和前端的状态一致性做的不够好。 比如SSE在一个session中进行，顶栏项目有状态变化， 但是session card 没有。 我切换session再切回来，流就不再继续了，只能来回切换最终看到llm输出完的最终结果，但是最后所有前端很多状态都跟不上，比如顶栏的项目红点还在， chat panel 的输入提交按钮还是红色（打断）的状态。 这里我觉得可能要重新思考前端sse的渲染架构。
