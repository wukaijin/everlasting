# Rust
/target
**/*.rs.bk
Cargo.lock.bak
**/*.pdb

# Tauri
**/tauri/target
**/tauri.conf.json.bak

# Node / pnpm
node_modules/
.pnpm-store/
dist/
build/
.next/
out/
*.tsbuildinfo
.vite/

# 依赖 lock(允许提交 pnpm-lock.yaml / Cargo.lock,这两个是项目稳定性的关键,保留)
# 但本地临时 lock 备份忽略
*.lock.bak

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
*.iml

# OS
.DS_Store
Thumbs.db
desktop.ini

# 本地环境 / 临时
*.log
*.tmp
.env.local
.env.*.local
.envrc.local

# 本地开发临时目录(早期 pip install fonttools 测试残留,现改用 Node 工具链)
.venv/
scripts-tmp-node_modules/

# 字体源文件(可从上游仓库重新下载,不需要纳入版本控制;subset 产物 woff2 已提交)
app/src/assets/fonts/source/

# 数据库 / 缓存(项目运行后会产生,不入库)
*.db
*.db-journal
*.db-shm
*.db-wal
*.sqlite
*.sqlite-journal
.cache/

# App runtime spillover only (shell tool outputs at `<cwd>/.everlasting/outputs/`).
# Project-level config under `.everlasting/agents/` (L3d subagents), `commands/` (B3),
# and `skills/` (B4) is **committed and shared** — see loader.rs docs.
.everlasting/outputs/


reasonix.toml
.codegraph/
